{
  "private": true,
  "license": "MIT",
  "scripts": {
    "preview": "./scripts/preview.sh",
    "clear-preview": "./scripts/clear-preview.sh",
    "compile": "npm run clean && $(npm bin)/harp compile src dist",
    "publish": "npm run compile && bin/publish",
    "clean": "rm -rf ./dist"
  },
  "devDependencies": {
    "harp": "^0.20.0",
    "gh-pages": "^0.9.0"
  }
}
